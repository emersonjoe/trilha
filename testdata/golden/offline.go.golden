// Code generated by trilha. DO NOT EDIT.

//go:generate trilha gen

package main

import (
	app "example.com/offline/app"
	app_api_itens "example.com/offline/app/api/itens"
	app_campo "example.com/offline/app/campo"
	trilha "github.com/emersonjoe/trilha"
)

// newApp builds the application. Tests can call it and use a.Handler().
func newApp() *trilha.App {
	cfg := trilha.ConfigFromEnv()
	cfg.Public = trilha.PublicFS(nil, "public")
	a := trilha.New(cfg)
	a.Register(trilha.Route{
		Pattern: "/",
		Page:    app.Page,
	})
	a.Register(trilha.Route{
		Pattern: "/api/itens",
		Methods: map[string]trilha.HandlerFunc{
			"GET": app_api_itens.GET,
		},
	})
	a.Register(trilha.Route{
		Pattern: "/campo",
		Page:    app_campo.Page,
		Offline: true,
		Methods: map[string]trilha.HandlerFunc{
			"POST": app_campo.POST,
		},
	})
	return a
}

func main() {
	trilha.Run(newApp())
}
