// Code generated by trilha. DO NOT EDIT.

//go:generate trilha gen

package main

import (
	app "example.com/kindtree/app"
	app_api "example.com/kindtree/app/api"
	app_api_coisas "example.com/kindtree/app/api/coisas"
	app_api_coisas_propria "example.com/kindtree/app/api/coisas/propria"
	app_formularios "example.com/kindtree/app/formularios"
	app_formularios_fundo "example.com/kindtree/app/formularios/fundo"
	app_paginas "example.com/kindtree/app/paginas"
	trilha "github.com/emersonjoe/trilha"
)

// newApp builds the application. Tests can call it and use a.Handler().
func newApp() *trilha.App {
	cfg := trilha.ConfigFromEnv()
	cfg.Public = trilha.PublicFS(nil, "public")
	a := trilha.New(cfg)
	a.Register(trilha.Route{
		Pattern: "/",
		Page:    app.Page,
	})
	a.Register(trilha.Route{
		Pattern: "/api/coisas",
		Kind:    app_api.Kind,
		Methods: map[string]trilha.HandlerFunc{
			"GET": app_api_coisas.GET,
		},
	})
	a.Register(trilha.Route{
		Pattern: "/api/coisas/propria",
		Kind:    app_api_coisas_propria.Kind,
		Methods: map[string]trilha.HandlerFunc{
			"POST": app_api_coisas_propria.POST,
		},
	})
	a.Register(trilha.Route{
		Pattern: "/formularios",
		Kind:    app.Kind,
		Methods: map[string]trilha.HandlerFunc{
			"POST": app_formularios.POST,
		},
	})
	a.Register(trilha.Route{
		Pattern: "/formularios/fundo",
		Kind:    app.Kind,
		Methods: map[string]trilha.HandlerFunc{
			"POST": app_formularios_fundo.POST,
		},
	})
	a.Register(trilha.Route{
		Pattern: "/paginas",
		Page:    app_paginas.Page,
	})
	return a
}

func main() {
	trilha.Run(newApp())
}
