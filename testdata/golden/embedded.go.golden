// Code generated by trilha. DO NOT EDIT.

//go:generate trilha gen

package crm

import (
	app "example.com/embedded/app"
	app_contatos "example.com/embedded/app/contatos"
	trilha "github.com/emersonjoe/trilha"
)

// NewApp builds the application. The host binary calls it and mounts the
// result: mux.Handle("/", NewApp().Handler()).
func NewApp() *trilha.App {
	cfg := trilha.ConfigFromEnv()
	cfg.Public = trilha.PublicFS(nil, "public")
	a := trilha.New(cfg)
	a.Register(trilha.Route{
		Pattern: "/",
		Page:    app.Page,
	})
	a.Register(trilha.Route{
		Pattern: "/contatos",
		Page:    app_contatos.Page,
		Methods: map[string]trilha.HandlerFunc{
			"POST": app_contatos.POST,
		},
	})
	return a
}
