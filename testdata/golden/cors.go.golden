// Code generated by trilha. DO NOT EDIT.

//go:generate trilha gen

package main

import (
	app "example.com/cors/app"
	app__well_known_oauth_protected_resource "example.com/cors/app/.well-known/oauth-protected-resource"
	app_api "example.com/cors/app/api"
	trilha "github.com/emersonjoe/trilha"
)

// newApp builds the application. Tests can call it and use a.Handler().
func newApp() *trilha.App {
	cfg := trilha.ConfigFromEnv()
	cfg.Public = trilha.PublicFS(nil, "public")
	a := trilha.New(cfg)
	a.Register(trilha.Route{
		Pattern: "/",
		Page:    app.Page,
	})
	a.Register(trilha.Route{
		Pattern: "/.well-known/oauth-protected-resource",
		CORS:    &app__well_known_oauth_protected_resource.CORS,
		Methods: map[string]trilha.HandlerFunc{
			"GET": app__well_known_oauth_protected_resource.GET,
		},
	})
	a.Register(trilha.Route{
		Pattern: "/api",
		Methods: map[string]trilha.HandlerFunc{
			"GET":     app_api.GET,
			"OPTIONS": app_api.OPTIONS,
		},
		MiddlewaresByMethod: map[string][]trilha.MiddlewareFunc{
			"OPTIONS": {app_api.MiddlewareOPTIONS},
		},
	})
	return a
}

func main() {
	trilha.Run(newApp())
}
