# Tarefas — Spec 145

- [x] T001 `TestExternalSubcommand`: função `runExternal` e binário (`cmd/trilha/external_test.go`)
- [x] T002 Despacho em `main.go`; linha de uso en/pt em `i18n.go` (sem citar `trilha <palavra>`, que o `TestAgentsMatchesUsage` leria como comando)
- [x] T003 Cache em `.trilha/cache/`: `internal/dev/server.go`, `cmd/trilha/export.go`, scaffold, `.gitignore`, mensagem do `audit`
- [x] T004 Três capítulos en + pt e slugs em `docs.go`; convenções (`.trilha/`) nas duas locales
- [x] T005 `CHANGELOG.md`, `version` em `main.go`, item do `ROADMAP.md`
- [ ] T006 `make test` verde e `make release VERSION=0.124.0 ISSUES="232"`
