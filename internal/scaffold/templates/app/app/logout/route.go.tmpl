// Package logout ends the session. It is a POST because a link that logs
// somebody out is a link anybody can put in an image tag.
package logout

import (
	"github.com/emersonjoe/trilha"

	"{{.Module}}/internal/session"
)

// POST /logout
func POST(c *trilha.Ctx) error { return session.Flow.Logout(c) }
